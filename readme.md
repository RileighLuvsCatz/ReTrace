# ReTrace

A screentime and app usage tracker for Linux and Windows.

## CLI

```sh
retrace daemon start
retrace daemon status
retrace daemon stop

retrace today
retrace week
retrace app Editor
retrace stats
```

Use `--db <path>` to read or write a database outside the default `trace.db`.
